# ThingsManager 服务端可选环境变量（systemd EnvironmentFile）
# 改完执行：systemctl restart thingsmanager
#
# 说明：默认监听 0.0.0.0:3200、数据目录 /var/lib/thingsmanager（由 runtime.config.json 决定）。
#       这里的变量优先级高于运行配置，一般无需修改——端口/监听地址请在页面「系统设置 → 桌面/网络」里改。
#
# THM_PORT=3200
# THM_HOST=0.0.0.0
# THM_DATA=/var/lib/thingsmanager
