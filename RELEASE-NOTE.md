「药品管理」新增「物资编码」「产品批号」两列（新增 / 编辑 / 按模板导入都能填，搜索按物资编码、批号也能查）；药品现在可以走出入库单——在出入库页按药品名称或物资编码即可选中（候选中标着「药品」），单据照常生成 / 打印 / 导出，出库同样校验库存，做过出入库后药品管理页会显示结存与最近一次出入库；药品不进入「物资管理」「库存 & 清单」「盘库」，台账仍只在药品管理页；「分类设置」新增「常用单位」——客户 / 领用部门 / 借用人等非本地部门可维护成清单，出入库单、借用单、单据更正等处提供下拉候选，仍可直接手写；列表统一把「物资编码」放在第一列（药品管理、坏件管理、盘库明细、借用台账等）；出入库页的「拍照 / 上传图片」「选择导入文件」点下去即时给出反馈，手机大照片先在本地压缩再上传，识别更快更稳；修正操作日志里的来源 IP：以发起访问的设备真实地址为准，不再把链路中间的代理 / 网关地址当成来源
